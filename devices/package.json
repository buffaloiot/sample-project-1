{
	"name": "device",
	"description": "device emulator",
	"version": "0.0.1",
	"main": "dist/device/index.js",
	"bin": {
		"device": "./dist/device/index.js"
	},
	"scripts": {
		"compile": "mkdir -p dist && tar -cf - src | tar -C dist -xf - --strip 1"
	},
	"dependencies": {
		"mqtt": "^4.2.1",
		"minimist": "^1.2.5"
	}
}
